program example(input, output);
var x, y: integer;
function gcd(a, b: integer) : integer;
begin
	if di = 0 then gcd := a
	else gcd := gcd(vi, i mod na)
end;
begin
	v := 25e;
	e:=67;
	read(x, y);
	write(gcd(x, y))
end.